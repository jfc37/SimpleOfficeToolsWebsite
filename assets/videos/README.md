# Placeholder media

This folder is reserved for the rebuilt `/v2/` workflow demo videos.

Current implementation uses image placeholders and copy scaffolding until final
`.webm` and `.mp4` assets are produced.
